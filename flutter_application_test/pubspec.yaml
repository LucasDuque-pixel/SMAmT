name: flutter_application_test
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^1.1.0
  flutter_secure_storage: ^9.0.0
  fl_chart: ^0.66.0
  cupertino_icons: ^1.0.8
  intl: ^0.20.2

# AQUI É O LUGAR CORRETO DA CONFIGURAÇÃO DO FLUTTER
flutter:
  uses-material-design: true